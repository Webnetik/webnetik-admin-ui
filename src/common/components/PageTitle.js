import React from 'react';
import { PageHeader } from 'antd';

const pageTitle = ({title}) => {
    return(
        <PageHeader
            title={title}
        />
    );
};

export default pageTitle;
